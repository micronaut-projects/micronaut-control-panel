{{#with ext.controlPanel as |controlPanel|}}
<section class="cp-detail-shell">
    <div class="cp-detail-body">
        {{> (lookup . 'controlPanel.detailedView.file') parameter=controlPanel }}
    </div>
</section>
{{/with}}
