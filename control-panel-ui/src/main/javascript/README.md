Main documentation: https://codemirror.net/examples/bundle/

1. Install Rollup:
   ```bash
   npm i -g rollup @rollup/plugin-node-resolve
   ```
2. Generate the bundle:
   ```bash
   rollup -c rollup.config.mjs
   ```
