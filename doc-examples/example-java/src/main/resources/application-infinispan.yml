infinispan:
  enabled: true
  client:
    hotrod:
      force-return-values: true
      security:
        authentication:
          realm: default
          sasl-mechanism: DIGEST-SHA-512
          server-name: infinispan
