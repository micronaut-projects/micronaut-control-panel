<p class="card-text">
    <strong>{{badge}}</strong> routes loaded.
</p>
{{#with controlPanel.body }}
    {{#if openApiViewerUri}}
        <p class="card-text">
            <a class="btn btn-secondary" role="button" href="{{openApiViewerUri}}" target="_blank">
                <i class="fas fa-book-open"></i>&nbsp;OpenAPI viewer
            </a>
        </p>
    {{/if}}
{{/with}}
