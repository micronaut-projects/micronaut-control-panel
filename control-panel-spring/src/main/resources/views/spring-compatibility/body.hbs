{{#with controlPanel.body }}
    <div class="d-flex flex-column gap-2">
        <div><strong>{{ beanCount }}</strong> Spring-origin beans</div>
        <div><strong>{{ routeCount }}</strong> Spring-origin routes</div>
        <div><strong>{{ endpointCount }}</strong> actuator endpoint operations</div>
    </div>
{{/with}}
