{{#with controlPanel.body }}

    {{> "views/datasource/detail-scripts" }}

    <div class="cp-datasource-page">
        <div class="tabs cp-datasource-tabs" data-tabs data-datasource-tabs>
            {{> "views/datasource/detail-tabs" }}
            {{> "views/datasource/detail-tables-panel" }}
            {{> "views/datasource/detail-query-panel" }}
            {{#if poolInfo}}
                {{> "views/datasource/detail-pool-panel" }}
            {{/if}}
        </div>
    </div>

{{/with}}
